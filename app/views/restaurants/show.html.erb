<h1><%= @restaurant.name %></h1>

<ul>
  <li>
    <%= @restaurant.address %>
  </li>
  <li>
    <%= @restaurant.city %>
    <%= @restaurant.state %>
    <%= @restaurant.zip_code %>
  </li>
</ul>

<% if current_user %>
  <%= button_to "Delete", @restaurant, method: :delete, data:
  {confirm: "Are you sure you want to delete this restaurant?"}%>
<% end %>

<p class="text-center"><%= link_to "Home", restaurants_path %></p>
